from tqdm import  tqdm
from time import sleep
from tkinter import *
from tkinter import messagebox
#Barra de progresso

for p in tqdm(range(20)):
    sleep(1.0)

if range(20):
    messagebox.showinfo("Comcluido!!")
